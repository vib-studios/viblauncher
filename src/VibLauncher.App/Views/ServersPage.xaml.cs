using System.Windows.Controls;

namespace VibLauncher.App.Views;

/// <summary>Code-behind for ServersPage. The page's behaviour lives in its view model.</summary>
public partial class ServersPage : UserControl
{
    public ServersPage() => InitializeComponent();
}
